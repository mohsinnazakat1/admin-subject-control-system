  <%= form_with model: @quiz do |form| %>
  <div>
    <%= form.label :title %><br>
    <%= form.text_field :title %>
    <br>
    <%= form.select :chapter_id, Chapter.all.map{ |c| [c.title, c.id] } %>

    <% @quiz.errors.full_messages_for(:title).each do |message| %>
      <div><%= message %></div>
    <% end %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
