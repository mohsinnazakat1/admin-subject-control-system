<h1>Grades</h1>

<% if user_signed_in? %>
  <li class="nav-item active">
    <%= link_to "Sign Out", destroy_user_session_path, :method=> :delete %>
  </li>
<% end %>

<ul>
  <% @grades.each do |grade| %>
    <li>
    <%= link_to grade.title, grade %>
    </li>

  <% end %>
</ul>

<%= link_to "Add a grade", new_grade_path %>

