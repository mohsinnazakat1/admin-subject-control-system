<ul>
  <li><%= link_to "Edit", edit_note_path(@note) %></li>
  <li><%= link_to 'Destroy', note_path(@note),
              method: :delete,
              data: { confirm: 'Are you sure?' 
                  } %></li>
</ul>

<h1> <%= @note.title %> </h1>
<h2> <%= @note.body %> </h2>

<%= link_to "All Notes", notes_path %>
